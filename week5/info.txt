2 < 3 ? '참' : '거짓'

var num = prompt("값 입력");
-> 입력창이 팝업되어 num에 값 직접 입력

var num = confirm("확인하셨습니까?");
-> 팝업이 뜨며 확인(true)과 취소(false)로 버튼 입력

var은 변수(나중에 변경가능)
let은 상수(나중에 변경불가)(재할당 가능)
const도 상수지만 재할당 안 됨

웬만해서 상수는 const로 하고 재할당이 필요한 경우에만 let 사용 권장
